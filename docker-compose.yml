services:
  base_rails_7:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ../..:/workspaces:cached
    working_dir: /workspaces/base_rails_7
    command: bash -c "rm -f tmp/pids/server.pid && bin/rails server -b 0.0.0.0 -p 3000"
    networks:
      - base_rails_7_network
    ports:
      - 3000:3000
    environment:
      RAILS_ENV: "development"
      DB_HOST: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      REDIS_URL: redis://redis:6379/0
    depends_on:
      - redis
      - postgres

  postgres:
    image: postgres:16.1
    ports:
      - 5432:5432
    volumes:
      - base_rails_7_postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - base_rails_7_network

  redis:
    image: redis:7.4
    ports:
      - "6379:6379"
    networks:
      - base_rails_7_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 2s

  sidekiq:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ../..:/workspaces:cached
    working_dir: /workspaces/base_rails_7
    command: bash -c "until redis-cli -h redis ping; do echo 'Esperando Redis...'; sleep 2; done; bundle exec sidekiq"
    networks:
      - base_rails_7_network
    environment:
      RAILS_ENV: "development"
      DB_HOST: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_started

volumes:
  base_rails_7_postgres_data:

networks:
  base_rails_7_network:
    name: base_rails_7_network
