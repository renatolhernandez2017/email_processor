<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta property="og:title" content="email_processor" />
    <meta property="og:description" content="" />
    <meta property="og:image" content="" />
    <meta property="og:url" content="" />
    <meta property="og:type" content="website" />

    <title>email_processor</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,200,0,0" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <script type="module">
      document.addEventListener("turbo:load", () => {
        const id = <%= 1 || 'null' %>;
        console.log(id);
        if (id) {
          window.subscribeToEmail(id);
        }
      });

      document.addEventListener("DOMContentLoaded", () => {
        const id = <%= 1 || 'null' %>;
        if (id) {
          window.subscribeToEmail(id);
        }
      });
    </script>
  </head>

  <body class="bg-base-200 h-screen">
    <div id="flash-message" data-controller="home" data-home-timeout-value="6000" class="w-full fixed z-50">
      <% flash.each do |type, message| %>
        <% daisy_class = { notice: "alert-info", success: "alert-success", error: "alert-error", alert: "alert-warning" }[type.to_sym] || "alert-info" %>

        <div role="alert" class="alert <%= daisy_class %> shadow-lg p-4 gap-2 w-[98%] text-black mx-auto mt-5">
          <span><%= message %></span>

          <div class="flex justify-end w-full">
            <button type="button" data-action="click->home#close" class="btn btn-ghost">✕</button>
          </div>
        </div>
      <% end %>
    </div>

    <%= render "shared/navbar" unless params[:controller].include?('users/sessions') %>
    <%= render "shared/notification_card" %>

    <%= yield %>
  </body>
</html>
