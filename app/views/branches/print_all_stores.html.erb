<div class="mb-20">
  <div class="grid grid-cols-12 mx-10 my-7">
    <div class="col-start-1 col-end-11">
      <h1 class="text-2xl font-bold text-primary text-center">Depósitos em bancos</h1>
    </div>

    <div class="col-start-12">
      <%= link_to "Download PDF", download_pdf_branches_path(kind: "print_all_stores"),
          target: "_blank", class: "btn btn-primary w-36"
      %>
    </div>
  </div>

  <% @branches.each do |name, id, branch_number| %>  
    <section class="flex flex-col p-5">
      <div class="card shadow-lg border border-primary p-5">
        <label class="text-lg">
          <span class="text-primary"><%= name.upcase %> - </span>
          <span class="text-primary"><%= @closing %></span>
        </label>

        <%= render "total_orders", loose: @loose[id] if @loose.present? %>
        <%= render "billings", branch_number: branch_number, total_revenue: @total_revenue[id], with_partnership: @with_partnership[name] if @with_partnership.present? %>
        <%= render "full_partnership", with_partnership: @with_partnership[name] %>
        <%= render "other_informations", with_partnership: @with_partnership[name] %>
        <%= render "commission_payments", with_partnership: @with_partnership[name] %>
      </div>
    </section>
  <% end %>
</div>
