<section class="flex flex-col p-5 mb-20 gap-5">
  <% @representatives.each do |representative| %>
    <%= render "sub_actions", representative: representative, title: "Listagem de Pacientes", kind: "patient_listing" %>

    <%= render "shared/patient/listing", closing: @current_closing.closing, representative_name: representative.name,
        prescribers: @prescribers[representative.id]
    %>
  <% end %>
</section>
