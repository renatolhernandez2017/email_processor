<div class="modal-box max-w-fit overflow-auto">
  <form method="dialog" title="<%= @btn_back %>">
    <button class="btn btn-sm btn-circle hover:bg-gray-300 absolute right-2 top-2">x</button>
  </form>

  <%= @form.html_safe %>

  <button class="btn block relative -top-20 left-5 hover:bg-gray-300" type="button" onclick="this.closest('dialog').close()">
    <%= @btn_back %>
  </button>
</div>
