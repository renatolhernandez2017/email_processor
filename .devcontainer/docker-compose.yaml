services:
  unipharmus:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ../..:/workspaces:cached
    working_dir: /workspaces/unipharmus_v2
    command: bash -c "rm -f tmp/pids/server.pid && bin/rails server -b 0.0.0.0 -p 3003"
    networks:
      - unipharmus_network
    ports:
      - 3003:3003
    environment:
      RAILS_ENV: "development"
      DB_HOST: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      REDIS_URL: redis://redis:6379/0
    depends_on:
      - redis
      - postgres

  postgres:
    image: postgres:16.1
    ports:
      - 5432:5432
    volumes:
      - unipharmus_postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - unipharmus_network

  redis:
    image: redis:7.4
    ports:
      - "6379:6379"
    networks:
      - unipharmus_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 2s

  sidekiq:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ../..:/workspaces:cached
    working_dir: /workspaces/unipharmus_v2
    command: bash -c "until redis-cli -h redis ping; do echo 'Esperando Redis...'; sleep 2; done; bundle exec sidekiq"
    networks:
      - unipharmus_network
    environment:
      RAILS_ENV: "development"
      DB_HOST: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_started

volumes:
  unipharmus_postgres_data:

networks:
  unipharmus_network:
    name: unipharmus_network
